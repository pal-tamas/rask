@using Microsoft.AspNetCore.Components
@* Web is what carries BindAttributes — the [BindElement] descriptors that make @bind on a plain
   <input> lower to value= plus onchange. Without it the compiler has no binder for the element and
   emits a literal @bind attribute, which renders as markup and silently does nothing. *@
@using Microsoft.AspNetCore.Components.Web
