namespace Rask.Html.Components;

/// <summary>
///     <c>&lt;!DOCTYPE html&gt;</c>. Rendered by <c>HtmlSerializer</c> from
///     <see cref="Rask.Core.Components.DoctypeComponent" />, the base Core keeps so it can emit the
///     declaration without reaching into this assembly.
/// </summary>
public sealed partial class Doctype : DoctypeComponent;
