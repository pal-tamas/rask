<Project>

  <!--
    Rask.Wasm build integration.

    A consuming WASM project (any Microsoft.NET.Sdk project that sets
    <WasmGenerateAppBundle>true</WasmGenerateAppBundle> and references
    Rask.Wasm) gets two things wired in here:
      1. <WasmMainJSPath> set to the framework's main.js (unless the
         consumer has explicitly overridden it).
      2. <WasmExtraFilesToDeploy> appended with the framework's
         rask.wasm.js so it lands in the AppBundle alongside main.js.

    This removes the per-app wwwroot/ + CopyRaskWasmJs boilerplate that
    earlier consumers had to maintain by hand.

    Auto-imported in this repo via Directory.Build.targets at the repo root.
    Downstream NuGet consumers get the same auto-import via the build/ folder
    packed into the .nupkg. Both paths import unconditionally; the items are
    gated by <WasmGenerateAppBundle> so non-WASM consumers pay nothing.
  -->

  <PropertyGroup Condition=" '$(WasmGenerateAppBundle)' == 'true' AND '$(WasmMainJSPath)' == '' ">
    <WasmMainJSPath>$(MSBuildThisFileDirectory)..\Browser\main.js</WasmMainJSPath>
  </PropertyGroup>

  <ItemGroup Condition=" '$(WasmGenerateAppBundle)' == 'true' ">
    <WasmExtraFilesToDeploy Include="$(MSBuildThisFileDirectory)..\Browser\rask.wasm.js"/>
    <!-- Default page shell for standalone `dotnet run` on a Rask WASM project (WasmAppHost
         opens index.html). When served via Rask.Wasm.Hosting, this file is never used
         because UseRask<TApp>() prerenders the App component for every GET via MapFallback. -->
    <WasmExtraFilesToDeploy Include="$(MSBuildThisFileDirectory)..\Browser\index.html"/>

    <!--
      Roots EnumerateAll + the kindred bake-only public surface so the trimmer doesn't
      drop them in release-mode WASM publishes. Without this the bake task at publish
      time loads a trimmed Rask.Core.dll, fails to find EnumerateAll, logs the API-
      mismatch warning, and the AppBundle ships without /_rask/a/ files.
    -->
    <TrimmerRootDescriptor Include="$(MSBuildThisFileDirectory)Rask.Wasm.Trim.xml"/>
  </ItemGroup>

  <!--
    Publish-time scoped-asset bake. After the WASM SDK has assembled the AppBundle
    directory (which contains the trimmed assemblies in _framework/), walk every .dll,
    force the generator-emitted __RaskScopedCss/JsRegistration classes to populate the
    in-MSBuild ScopedAssetRegistry, then write each registered entry to
    {AppBundleDir}/_rask/a/{hash}.{ext}. The static-file mechanism in WasmAppHost (or
    any plain-static host) then serves what the in-process Rask.Wasm.Hosting endpoint
    would otherwise serve — closing the gap that caused the standalone-WASM E2E
    failures (per-component scripts 404'd, window.Rask.{TypeName} never populated,
    Rask.* invokes timed out via the 5s namespace poll).

    The task is loaded from a sibling DLL produced by Rask.Wasm.Tasks (the project's
    Build target copies it here). Runs on any build that produced an AppBundle —
    plain `dotnet build` for standalone-WASM dev, `dotnet publish` for release.
  -->
  <!--
    Do NOT gate this Condition on Exists(...Rask.Wasm.Tasks.dll). A UsingTask Condition is
    evaluated at project-evaluation time, which precedes the build-order edge (Rask.Wasm's
    ReferenceOutputAssembly="false" reference to Rask.Wasm.Tasks) that produces and copies the
    DLL here. The DLL is a generated artifact (gitignored), so on a clean checkout it is absent
    at evaluation — an Exists guard here would skip registration, then the invocation targets'
    own Exists guards (now true) would fire BakeScopedAssetsTask against an unregistered task →
    MSB4036. AssemblyFile-based UsingTasks load lazily (only when the task is invoked), and the
    two invocation targets below keep their Exists guards, so registering unconditionally is safe.
  -->
  <UsingTask TaskName="Rask.Wasm.Tasks.BakeScopedAssetsTask"
             AssemblyFile="$(MSBuildThisFileDirectory)Rask.Wasm.Tasks.dll"
             Condition=" '$(WasmGenerateAppBundle)' == 'true' "/>

  <!--
    Hook after _WasmGenerateAppBundle (the actual SDK target that produces the bundle
    contents). WasmGenerateAppBundle is the boolean enable-property, not a target name —
    binding AfterTargets to it would resolve to nothing. $(WasmAppDir) is the public
    property set by the SDK to the AppBundle output directory; both build and publish
    paths populate it.
  -->
  <!--
    Load assemblies from $(OutDir) — the build-output folder under bin/, not the
    publish/ subfolder. During Release `dotnet publish` the publish-context
    @(_WasmAssembliesInternal) points at IL-trimmed copies whose module
    initializers throw when invoked outside the WASM runtime (trimmed types they
    reference may be absent). The build-output copies are the same .NET metadata
    pre-trim, with all type references intact, and their [ModuleInitializer]s
    re-fire cleanly inside the MSBuild host. The .dll metadata we need is identical
    across configurations because the source generator is deterministic.
  -->
  <ItemGroup Condition=" '$(WasmGenerateAppBundle)' == 'true' ">
    <_RaskBakeInputAssemblies Include="$(OutDir)*.dll" Exclude="$(OutDir)publish/**;$(OutDir)AppBundle/**"/>
  </ItemGroup>

  <!--
    Build-time bake. Hooks BOTH _WasmGenerateAppBundle (the SDK target that fills the
    bundle) and the generic Build target. The second hook matters because a solution
    build can satisfy _WasmGenerateAppBundle for Rask.Example.Wasm via a concurrent
    nested `dotnet publish` (fired by Rask.Wasm.Hosting's _RaskPublishWasmBundle from
    every host/test project that references the WASM app). When that happens the direct
    build's _WasmGenerateAppBundle is incrementally skipped — so an AfterTargets hook on
    it alone never fires against the served bin/.../AppBundle, and a late concurrent
    clean+regen can strip the _rask/a files written by another worker. AfterTargets=Build
    guarantees one bake after this project's own build settles. The task overwrites and
    is idempotent, so a double-bake is a cheap no-op. Guarded on Exists($(WasmAppDir)) so
    it's a no-op when no bundle was produced.
    NOTE: the standalone-WASM E2E fixture serves the bundle via `dotnet run` with the
    no-build flag, which can re-stage the bundle after this build — see
    _RaskBakeScopedAssetsBeforeRun.
  -->
  <Target Name="_RaskBakeScopedAssets"
          AfterTargets="_WasmGenerateAppBundle;Build"
          Condition=" '$(WasmGenerateAppBundle)' == 'true' AND Exists('$(MSBuildThisFileDirectory)Rask.Wasm.Tasks.dll') AND '$(WasmAppDir)' != '' AND Exists('$(WasmAppDir)') ">
    <BakeScopedAssetsTask BundleDir="$(WasmAppDir)" Assemblies="@(_RaskBakeInputAssemblies)"/>
  </Target>

  <!--
    Run-time bake. `dotnet run` on a Rask WASM project launches WasmAppHost over the
    served AppBundle. It computes the launch via the SDK's ComputeRunArguments target
    (which the Run target depends on) even when the Build is skipped, so hooking
    AfterTargets="ComputeRunArguments" fires in both `dotnet run` and `dotnet run` with
    the no-build flag — unlike a hook on Run itself, which the no-build path does not
    execute. This re-bakes the _rask/a files into the exact directory about to be served,
    making the served bundle immune to whatever concurrent-build race left it in.

    $(WasmAppDir) is empty at this point (the SDK only sets it inside a build), so we
    compute the served bundle dir the same way the SDK's RunWorkingDirectory resolves:
    $(OutDir)AppBundle (absolute, via the project dir). The build-output assemblies the
    bake reads ($(OutDir)*.dll) are present from the prior build.

    FailOnEmpty=true: if the registry resolves but nothing bakes, fail fast with a clear
    error instead of serving a bundle that 404s on every scoped-asset URL.
  -->
  <Target Name="_RaskBakeScopedAssetsBeforeRun"
          AfterTargets="ComputeRunArguments"
          Condition=" '$(WasmGenerateAppBundle)' == 'true' AND Exists('$(MSBuildThisFileDirectory)Rask.Wasm.Tasks.dll') ">
    <PropertyGroup>
      <_RaskRunBundleDir Condition=" '$(WasmAppDir)' != '' ">$(WasmAppDir)</_RaskRunBundleDir>
      <_RaskRunBundleDir Condition=" '$(_RaskRunBundleDir)' == '' ">$([System.IO.Path]::Combine('$(MSBuildProjectDirectory)', '$(OutDir)', 'AppBundle'))</_RaskRunBundleDir>
    </PropertyGroup>
    <BakeScopedAssetsTask BundleDir="$(_RaskRunBundleDir)"
                          Assemblies="@(_RaskBakeInputAssemblies)"
                          FailOnEmpty="true"
                          Condition=" Exists('$(_RaskRunBundleDir)') "/>
  </Target>

  <!--
    Optional sub-path bake. Set <RaskPathBase> in the consuming csproj (or pass
    /p:RaskPathBase=/myapp on the command line) to scope the AppBundle under a
    sub-path. We rewrite the framework's default <base href="/"> in the bundled
    index.html so:
      - main.js / _framework/* resolve correctly under the sub-path; and
      - WasmHostBuilder.CreateDefault's auto-detect (which reads document.baseURI
        via the rask.wasm.js getBasePath export and writes it to
        LiveOptions.PathBase) picks up the prefix on first paint, so every head-
        emitted /_rask/a/{hash}.{ext} URL ends up as /myapp/_rask/a/{hash}.{ext}.
    No-op when RaskPathBase is empty (the default), so root-served deploys keep
    today's <base href="/"> intact.

    Normalization: "myapp" / "/myapp" / "/myapp/" all become <base href="/myapp/">.
    "/" / "" stay as "/" (no rewrite). Matches RaskPath.Normalize in Rask.Core
    plus a trailing slash (HTML <base> requires it for relative-URL resolution).
  -->
  <Target Name="_RaskRewriteBaseHref"
          AfterTargets="_RaskBakeScopedAssets"
          Condition=" '$(WasmGenerateAppBundle)' == 'true' AND '$(RaskPathBase)' != '' AND '$(RaskPathBase)' != '/' AND Exists('$(WasmAppDir)index.html') ">
    <PropertyGroup>
      <_RaskNormalizedBase>$(RaskPathBase.TrimEnd('/'))</_RaskNormalizedBase>
      <_RaskNormalizedBase Condition=" !$(_RaskNormalizedBase.StartsWith('/')) ">/$(_RaskNormalizedBase)</_RaskNormalizedBase>
      <_RaskIndexHtml>$([System.IO.File]::ReadAllText('$(WasmAppDir)index.html'))</_RaskIndexHtml>
      <_RaskIndexHtml>$(_RaskIndexHtml.Replace('&lt;base href=&quot;/&quot;/&gt;', '&lt;base href=&quot;$(_RaskNormalizedBase)/&quot;/&gt;'))</_RaskIndexHtml>
    </PropertyGroup>
    <WriteLinesToFile File="$(WasmAppDir)index.html"
                      Lines="$(_RaskIndexHtml)"
                      Overwrite="true"
                      WriteOnlyWhenDifferent="true"/>
    <Message Importance="High"
             Text="Rask: rewrote &lt;base href&gt; to &quot;$(_RaskNormalizedBase)/&quot; in $(WasmAppDir)index.html"/>
  </Target>

</Project>
