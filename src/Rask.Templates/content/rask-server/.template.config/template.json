{
  "$schema": "http://json.schemastore.org/template",
  "author": "Tamás Pál",
  "classifications": [ "Web", "Rask", "Server" ],
  "identity": "Rask.Templates.Server",
  "name": "Rask Server App",
  "description": "An ASP.NET-hosted Rask app — server-side rendering with live updates over a WebSocket.",
  "shortName": [ "rask-server", "rask" ],
  "tags": {
    "language": "C#",
    "type": "project"
  },
  "sourceName": "Company.RaskServer",
  "preferNameDirectory": true,
  "defaultName": "RaskApp",
  "symbols": {
    "auth": {
      "type": "parameter",
      "datatype": "bool",
      "defaultValue": "false",
      "description": "Scaffold a cookie-backed login: a /login form, a credential store, a protected /members page, and the auth wiring."
    },
    "pwa": {
      "type": "parameter",
      "datatype": "bool",
      "defaultValue": "false",
      "description": "Make the app an installable PWA: AddRaskPwa serves a web app manifest + Rask's service worker and registers it. Installable + push-capable (Web Push), not an offline app — offline navigations show a static offline.html."
    },
    "cqrs": {
      "type": "parameter",
      "datatype": "bool",
      "defaultValue": "false",
      "description": "Scaffold the Rask.Cqrs mediator: a sample IQuery + handler, a /greeting page that injects IDispatcher and dispatches it, the Rask.Cqrs package reference, and the AddRaskCqrs() wiring."
    },
    "docker": {
      "type": "parameter",
      "datatype": "bool",
      "defaultValue": "false",
      "description": "Add a multi-stage Dockerfile + .dockerignore to containerize the app (build on the .NET SDK image, run on the aspnet:10.0 runtime)."
    }
  },
  "sources": [
    {
      "modifiers": [
        {
          "condition": "(!auth)",
          "exclude": [ "Auth/**" ]
        },
        {
          "condition": "(!pwa)",
          "exclude": [ "wwwroot/**" ]
        },
        {
          "condition": "(!cqrs)",
          "exclude": [ "Cqrs/**" ]
        },
        {
          "condition": "(!docker)",
          "exclude": [ "Dockerfile", ".dockerignore" ]
        }
      ]
    }
  ]
}
