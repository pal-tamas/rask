@using System.Globalization
@using Microsoft.AspNetCore.Components
@* Carries the [BindElement] descriptors. Without it, @bind on a plain <input> compiles to a literal
   attribute that renders as markup and silently does nothing. *@
@using Microsoft.AspNetCore.Components.Web
